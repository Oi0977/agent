# 01 · 文档解析(preprocessor)

> 解决的问题:把 PDF/Markdown 等各种格式的文档,变成**统一的纯文本**,
> 供下游分块。RAG 的第一站:解析质量决定后面一切的上限(垃圾进垃圾出)。

## 1. 核心机制:PDF 有两种,解析策略只有对应两种

PDF 本质是一个"容器"格式,内容怎么来的分两种情况:

| | 文本层 PDF | 光栅化 PDF |
|---|---|---|
| 来源 | Word/LaTeX 直接导出 | HTML 转换、扫描件、截图拼合 |
| 内部结构 | 每页有**字符对象**(带坐标/字体) | 页面只有嵌入图片 + 矢量路径,无字符对象 |
| 提取方式 | 直接读字符对象(快、100%准) | 必须渲染成图片 → OCR 识别 |

`pdfplumber`(基于 pdfminer.six)只能做第一种:它**读取** PDF 里已存在的文本层,
不做任何"识别"。对光栅化 PDF,`extract_text()` 返回空。

### 自适应判断(pdf_parser.py)

```python
MIN_TEXT_LENGTH = 10

def _has_text_content(page) -> bool:
    if len(page.chars) == 0:          # 字符对象都没有 → 光栅化页
        return False
    text = page.extract_text()
    if not text or len(text.strip()) < MIN_TEXT_LENGTH:
        return False                  # 有字符但少于10个 → 伪文本层(如只有页码)
    return True
```

每页独立判断——一本 600 页的书可能 590 页有文本层、10 页是插图页,各自走各自的路。

### OCR 降级路径

```python
def _ocr_page(page) -> str:
    page_img = page.to_image(resolution=150)   # 页面渲染成 150dpi 图片
    pil_img = page_img.original
    ocr = _get_ocr()                            # RapidOCR 单例
    result, _ = ocr(pil_img)                    # [[box, text, score], ...]
    return "\n".join(item[1] for item in result)
```

**要点**:
- `resolution=150` 是速度/质量的平衡点:再低识别率掉,再高(300dpi)慢一倍但收益小
- RapidOCR 返回的是"检测框列表",`item[1]` 是每个框里的文本,按文档阅读顺序拼接
- `_get_ocr()` 单例:OCR 引擎初始化要加载检测+识别两个模型(秒级),
  逐页调用绝不能重复初始化——单例是必须的,不是优化

## 2. RapidOCR 是什么、为什么选它

`rapidocr-onnxruntime` = **PaddleOCR 训练的 PP-OCRv3 模型 + ONNX Runtime 推理引擎**,
不依赖 paddlepaddle 本身。

选它的原因(Windows 环境约束):
- PaddlePaddle 3.x 在 Windows 有 oneDNN 兼容 bug,2.x 依赖链过长(imgaug/scipy 全家桶)
- ONNX Runtime 是独立推理引擎,只需 `onnxruntime` + `opencv-python` 两个依赖
- 识别效果与 PaddleOCR 一致(模型就是 PaddleOCR 训练的)

## 3. 踩坑记录

- **pdfplumber 不做 OCR**(常见误解):装了 pdfplumber ≠ 能读扫描件。OCR 能力来自
  RapidOCR,"哪页走 OCR"的判断是我们自己写的——这个判断逻辑是解析器的灵魂
- `page.to_image()` 需要 ghostscript 渲染支持(渲染,不是识别)

## 4. 业界选型与取舍

### 选型一:OCR / 解析引擎

| 候选 | 路线 | 优势 | 劣势 |
|------|------|------|------|
| PaddleOCR(原生) | 传统 OCR,检测+识别 | 生态最全 | paddlepaddle 3.x 有 Windows oneDNN bug;2.x 依赖链巨长 |
| **RapidOCR(本项目)** | 同源模型 + ONNX 推理 | 模型同源效果一致;依赖极轻;离线免费 | 无版面理解 |
| 云端 OCR API(百度/腾讯) | 传统 OCR 服务化 | 无本地资源占用 | 按量付费;数据外发;依赖网络 |
| Unstructured | 通用解析库 | 开箱支持几十种格式 | 黑盒,学习价值低;中文效果一般 |
| MinerU / DeepDoc | 版面模型混合路线 | **懂版面**(标题/表格/阅读顺序) | 重(带模型);对纯文本书籍收益小 |
| 生成式(Unlimited-OCR 类) | 端到端 VLM 输出结构化 | 效果天花板 | 3B 模型本地跑不动(8G 显存);API 付费 |

**本项目的取舍逻辑**:取舍标准按优先级是 ①Windows 可装可用 ②依赖轻 ③离线免费
④中文识别效果 ⑤学习透明(能看穿机制)。RapidOCR 在 ①②③⑤ 全占,④ 与 PaddleOCR
同源打平。生成式/版面模型效果更强,但本场景(中文文字版技术书)识别本身不是瓶颈,
版面收益小——**先够用,升级触发条件是"表格/复杂版面文档成为主力"**(那时上 MinerU)。

### 选型二:解析路线(文本层提取框架)

pdfplumber(裸写判断逻辑)胜出,标准是**学习透明性**——自适应降级的判断逻辑
(`_has_text_content`)自己写,才能真懂"文本层 vs 光栅化"的本质;Unstructured
把这一切包起来,快但学不到。

## 5. 接口契约

```
parse_document(file_path) → str          # loader 按扩展名分派
parse_pdf(file_path) → str               # 自适应:文本层/OCR
```

产出一个大字符串(整本书几十万字),交给《02-文本分块》切分。

## 6. Q&A 自测(先凭理解答,再对照上文;可交 AI 批改)

> 在"你的回答"下直接写,写完自查或让 AI 批改。

### Q1 · pdfplumber 能读扫描版 PDF 吗?为什么?
**难度: 基础** · 考点: 文本层 vs 光栅化的本质

> **你的回答**:
> pdfplumber无法读取扫描版pdf，pdf格式文件是一个容器，里面有存储文字的和图片的，pdfplumber只能通过标准协议读取里面的文字，图片类型读取了无法识别，相当于pdfplumber是有个能打开这个容器的钥匙，如果里面的内容不是文字他也没办法。

> [!tip] 批改 · 7/10
> ✅ "容器"概念正确；识别到图片无法读取。
> ❌ **偏差**："图片类型读取了无法识别"暗示 pdfplumber 会尝试读图片然后失败——实际它**根本不看图片**，只遍历字符对象列表(page.chars)。页面里有没有图片跟它无关。
> ❌ **遗漏**：缺降级路径回答——应补充"自己不能，但项目用 RapidOCR 作为后备"。
>
> **改进版**：pdfplumber 不能读扫描版 PDF。PDF 是容器格式，pdfplumber 只遍历字符对象列表(page.chars)——扫描件整页就是一张图片，没有字符对象，所以 extract_text() 返回空。判断依据是 `page.chars == 0`。本项目用 RapidOCR 作为降级路径：先把页面渲染成图片，再 OCR 识别文字。

---

### Q2 · 判断"无文本层"为什么不能只看 `page.chars == 0`,还要设 MIN_TEXT_LENGTH=10?什么页面会漏过第一层判断?
**难度: 机制** · 考点: 伪文本层

> **你的回答**:
> 这个不是很清楚，可能有些pdf是混合的？主要内容是图片，但是页码那些是文字？这种情况就会漏过第一层判断

> [!tip] 批改 · 6/10
> ✅ 直觉方向完全正确——"页码那些是文字"正是伪文本层的典型场景。
> ❌ **不够自信**：你其实已经答对了，只是说"不是很清楚"。页码、页眉页脚、水印等**非正文内容**也会产生字符对象，所以 page.chars != 0 但 extract_text() 可能只有"第 1 页"三个字。
> ❌ **缺具体例子**：什么页面会漏过第一层判断？封面页（只有书名几个字）、纯图片页但有页码、章首页（只有章节标题）。
>
> **改进版**：有些页面虽然不是正文（比如纯插图页），但页眉页脚或页码仍然是字符对象，page.chars != 0。如果只看 chars == 0，这些页会被误判为"有文本层"，结果 extract_text() 只返回"1"或"第 1 页"这种无意义内容。MIN_TEXT_LENGTH=10 就是为了过滤这种伪文本层——有字符但少于 10 个字，说明正文不在这里，应该降级到 OCR。典型场景：封面页、纯图片页但有页码、章首页。

---

### Q3 · `_get_ocr()` 为什么要做单例?如果每次 `_ocr_page` 都新建 RapidOCR 会发生什么?
**难度: 决策** · 考点: 资源初始化成本

> **你的回答**:
> rapidocr每次初始化需要较大的时间和资源，就像初始化jdbc数据库一样，对于这些对象，单例模式是一个公认的> 优秀实现方式。
> 但是为什么初始化成本大我就不知道了。

> [!tip] 批改 · 5/10
> ✅ JDBC 类比很好，方向正确。
> ❌ **核心缺失**："为什么初始化成本大"是这道题的**核心考点**，必须答出来。
> ❌ **缺量化**：一本 600 页的书，每页都初始化一次 = 600 × 1.5s ≈ 15 分钟纯浪费。
>
> **改进版**：RapidOCR 初始化要加载两个 ONNX 模型（检测模型 + 识别模型），每个几十 MB，涉及磁盘读取 + 内存分配 + 推理引擎初始化，耗时约 1-3 秒。一本 600 页的书如果每页都新建，就是 600×1.5s ≈ 15 分钟纯浪费。单例保证只初始化一次，后续所有页面复用同一个实例。类比 JDBC 连接池：建连接贵，复用便宜。

---

### Q4 · 传统 OCR 和生成式文档解析的本质区别是什么?"认字准"和"懂结构"哪个才是代际差距?为什么文本层完好的 PDF 也有结构化解析需求?
**难度: 面试** · 考点: 技术代际判断

> **你的回答**:
> 生成式文档解析懂文本的结构，如表格这些，能够更好的将这些内容原封不动的转译，在现在每个字都精确识别的情况下，懂结构就是新的发展需求了和突破了。

> [!tip] 批改 · 6/10
> ✅ "懂结构是新发展方向"方向正确。
> ❌ **缺机制对比**：题目问"本质区别是什么"——传统 OCR 是"逐框认字"（检测框→识别字符），不理解文档语义；生成式（VLM）是"整页看图说话"，模型理解"这是表格""这是标题"。
> ❌ **没回答结构化需求**：文本层完好的 PDF 也有结构化需求——比如 pdfplumber 提取表格变成无序纯文本，行列关系全丢了。
>
> **改进版**：传统 OCR 是"逐框认字"：先检测文字区域，再逐区域识别字符，输出散落的文本片段，不理解文档结构。生成式（VLM）是"整页看图说话"：整个页面当图片输入多模态大模型，模型理解语义——知道这是表格、标题、正文。代际差距在"懂结构"，不在"认字准"（传统 OCR 中文识别率已 98%+）。文本层完好的 PDF 也有结构化需求：比如表格，pdfplumber 提取出来变成无序纯文本，行列关系全丢了；结构化解析能保留"第 2 行第 3 列是 XX"这种关系。

---

### Q5 · 客户给你一批以表格为主的扫描件 PDF,当前方案的瓶颈出现在哪一步?你的升级路径是什么?为什么不一步到位?
**难度: 面试** · 考点: 方案边界与演进意识

> **你的回答**:
> 方案的瓶颈是质量，我会先试着使用ocr来尝试，如果质量无法接受的话再转成生成式检索，一步到位需要考虑到成本迁移问题，在企业中应该本质够用就好的原则，尽可能低成本。

> [!tip] 批改 · 5/10
> ✅ "够用就好、低成本"是正确的工程思维。
> ❌ **瓶颈太笼统**："质量"不具体。核心瓶颈是 RapidOCR **没有版面理解**，表格会被拆成散落的文字块，行列关系丢失。
> ❌ **升级路径不够具体**："先试着使用 OCR"——当前方案已经是 OCR 了（RapidOCR），应说"先试当前方案看效果，如果表格结构丢失严重"再升级。应分两步：① MinerU（版面模型，本地离线）→ ② VLM（效果天花板，需 GPU/付费）。
> ❌ **没回答"为什么不一步到位"**。
>
> **改进版**：瓶颈在 RapidOCR 没有版面理解——表格扫描件会被拆成散落的文字块，行列关系丢失。升级路径分两步：① 先试 MinerU（版面模型，能理解表格/标题/阅读顺序，本地离线免费）；② 如果效果还不够，上生成式 VLM（效果天花板，但需 GPU 或 API 付费）。不一步到位的原因：成本递增（MinerU 本地跑 vs VLM 需 GPU/付费）、当前场景（中文技术书）表格不是主力，等"表格成为主力"再触发升级。

---

### 批改总结

| 题 | 得分 | 薄弱点 |
|---|---|---|
| Q1 | 7/10 | 缺降级路径、容器机制不够精确 |
| Q2 | 6/10 | 直觉对但没自信展开、缺具体例子 |
| Q3 | 5/10 | 核心原因（模型加载）没答出来 |
| Q4 | 6/10 | 缺机制对比、没回答结构化需求 |
| Q5 | 5/10 | 瓶颈太笼统、升级路径不够具体 |

**总体：能理解概念但"为什么"的深度不够。** 重点补 Q3（模型加载机制）和 Q5（方案边界的精确描述）——这两类是面试高频题，要求能说清"具体卡在哪"而不只是"效果不好"。